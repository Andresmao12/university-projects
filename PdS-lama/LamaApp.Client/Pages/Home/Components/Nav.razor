
<nav class="navbar navbar-expand-lg navbar-light w-100 border-bottom fixed-top bg-white h-auto">
    <div class="container-fluid px-4 mt-3 mb-2">
        <div class="row w-100">
            <div class="col-12 col-md-6 d-flex align-items-center gap-4">
                <h1 class="m-0 fw-bolder fs-3">LAMA</h1>

                <ul class="navbar-nav gap-3">
                    <li class="nav-item">
                        <a class="nav-link active text-secondary" aria-current="page" href="#Home">Inicio</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link text-secondary" aria-current="page" href="#Episodes">Episodios</a>
                    </li>
                </ul>
               
            </div>
            <div class="col-12 col-md-6 d-flex align-items-center justify-content-end gap-4">
                <div class="inp-container d-flex flex-column">
                    @*
                    <label for="searchEpisodes">Search episodes</label>
                    *@

                    <input id="searchEpisodes" type="text" class="form-control" placeholder="Search episodes..." />
                </div>
                @* Icono perfil o componente Blazor para el perfil

                *@

                @* No sirve el modal para preguntar si iniciar sesion o registrarse *@
                <button class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#staticBackdrop" onclick="window.location.href = '/login'">
                    <i class="fa-solid fa-user"></i>
                </button>

                @* Botones temporales para ingresar

                <button class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#staticBackdrop" onclick="window.location.href = '/user'">
                Acceso al inicio
                </button>

                <button class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#staticBackdrop" onclick="window.location.href = '/admin/stats'">
                Acceso al admin
                </button>
                *@
                


               @* BOTON TRIGGER *@
                @* <button class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#modalSesion">
                    Launch static backdrop modal
                </button>

                    MODAL
                <div class="modal fade" id="modalSesion" tabindex="-1" aria-hidden="true" aria-labelledby="label-modal-1">

                    <div class="modal-dialog">
                        <div class="modal-content">
                            <div class="modal-header">
                                <h2 class="modal-title">Titulo</h2>
                            </div>
                            <div class="modal-body">
                                <h4>Contenido del modal</h4>
                            </div>

                        </div>
                    </div>
                </div> *@


            </div>
        </div>
    </div>
</nav>

@code {

}
